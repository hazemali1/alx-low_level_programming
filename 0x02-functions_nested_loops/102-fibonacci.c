#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A c program to printing
 *
 * Return: Always 0 mean it (sucess)
*/
int main(void)
{
	int s = 1;
	int d = 2;

	printf("%d, ", s, d);
	f = s + d;
	printf("%d, ", f);
	return (0);
}
