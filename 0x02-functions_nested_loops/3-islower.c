#include "main.h"

/**
 * _islower - Checks
 * Parameter: c
 *
 * Description: Checks alphabet
 *
 * Return: Return 0 (sucess)
*/
int _islower(int c)
	if (c >= 'a' && c <= 'z')
{
	return (1);
}
	else if (c >= 'A' && c <= 'Z')
{
	return (0);
}
