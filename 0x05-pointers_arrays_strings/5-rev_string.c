#include "main.h"

/**
 * rev_string - Prinnt
 *
 * Description: Rev
 *
 * @s: Parameter
 *
 * Return: Return 0 (sucess)
*/
void rev_string(char *s)
{
        int f, d = 0;

        while (s[d] != '\0')
        {
                d++;
        }
        for (f = d - 1; f >= 0; f--)
        {
		s[] = s[f];
        }
}
