#include <stdio.h>

/**
 * main - Get
 *
 * Description: Alpha
 *
 * Return: Returns 0 (sucess)
*/
int main(void)
{
	int x, s;
for (x = 97; x <= 122; x++)
{
	putchar(x);
}
for (s = 65; s <= 90; s++)
{
	putchar(s);
}
	putchar('\n');
	return (0);
}
